***********************
WEEK-2 QUIZ
***********************


--------------------------------------------------------------------------------------------------------------------------------------------------------
1.  Question.   In the struct
                    struct point{
                    double x;
                    double y;
                    } p1 = { 2.5, 4.0};
                The value of p1.x is

    Ans. 2.5
--------------------------------------------------------------------------------------------------------------------------------------------------------
2.  Question.   If we have pointer 
                    struct point* ptr = &p1;
                With p1 as
                    struct point{
                    double x;
                    double y;
                    } p1 = { 2.5, 4.0};
                Then ptr->y has the value

    Ans. 4.0
--------------------------------------------------------------------------------------------------------------------------------------------------------
3.  Question. Which is a basic elementary operation for a stack
    Ans. Empty
--------------------------------------------------------------------------------------------------------------------------------------------------------
4.  Question.   In the code snippet
                    struct point{
                    double x;
                    double y;
                    } p1 = { 4, 4.0};

                    printf("%f\n", p1.x);
                What gets printed?

    Ans. 4.000000
--------------------------------------------------------------------------------------------------------------------------------------------------------
5.  Question.   In the code snippet
                    struct point{
                    double x;
                    double y;
                    } p1 = {2.5, 4.0};

                    printf("%f\n", p1.x);
                What gets printed?

    Ans. 2.500000
--------------------------------------------------------------------------------------------------------------------------------------------------------
6.  Question.   If we have pointer 
                    struct point* ptr = &p1;

                With p1 as
                    struct point{
                    double x;
                    double y;
                    } p1;

                Then ptr->y has the value

    Ans.  0.0
--------------------------------------------------------------------------------------------------------------------------------------------------------
7.  Question.   If we have pointer 
                    struct point* ptr = &p1;

                With p1 as
                    struct point{
                    double x;
                    double y;
                    } p1 = {2.5};

                Then ptr->x has the value

    Ans. 2.5
--------------------------------------------------------------------------------------------------------------------------------------------------------
8.  Question.   What operation would you use to add something onto a stack?
    Ans. Push
--------------------------------------------------------------------------------------------------------------------------------------------------------
9.  Question.   If we push all the elements of the array int data[20]; onto a stack – the element at the bottom of the stack is
    Ans. data[0]
--------------------------------------------------------------------------------------------------------------------------------------------------------
10. Question.   If we have pointer 
                    struct point* ptr = &p1;
                With p1 as
                    struct point{
                    double x;
                    double y;
                    } p1 = {4, p1.x};
                Then ptr->y has the value

    Ans. 4
--------------------------------------------------------------------------------------------------------------------------------------------------------